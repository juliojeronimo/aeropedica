import React from 'react';

import { BookingDone } from '.'

export default {
    title: 'Aeropedica/BookingDone',
    component: BookingDone,
    argTypes: {
    },
};

const Template = (args) => <BookingDone/>;


export const Default = Template.bind({});

Default.args = {
  
};