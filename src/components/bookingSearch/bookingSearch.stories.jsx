import React from 'react';

import { BookingSearch } from './'

export default {
    title: 'Aeropedica/BookingSearch',
    component: BookingSearch,
    argTypes: {
    },
};

const Template = (args) => <BookingSearch/>;


export const Default = Template.bind({});

Default.args = {
  
};